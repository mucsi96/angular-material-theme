/*
 * Public API surface of @mucsi96/angular-material-theme.
 *
 * The theme is delivered primarily as SCSS — consumers should import
 * `@mucsi96/angular-material-theme/styles` (or the prebuilt CSS) from their
 * application's global stylesheet. A handful of optional Angular helpers are
 * exported below for convenience.
 */
export * from './lib/theme.providers';
